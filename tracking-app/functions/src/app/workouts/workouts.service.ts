import { createWorkout } from './workouts.repository';
/* 
export async function create(training: any) {
  if (!training.userId) {
    throw new Error('userId required');
  }

  return createWorkout({
    ...training,
    createdAt: new Date().toISOString(),
  });
}
 */